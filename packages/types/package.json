{
  "name": "@repo/types",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@repo/eslint-config": "*",
    "typescript": "^5.5.0"
  },
  "scripts": {
    "build": "tsc",
    "lint": "prettier --write . && eslint --fix ."
  }
}
