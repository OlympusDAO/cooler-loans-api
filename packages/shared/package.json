{
  "name": "@repo/shared",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/node": "^18",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "tsc",
    "lint": "prettier --write . && eslint --fix ."
  }
}
