export type Snapshot = {
  date: Date;
  // Top-level summary
  // Per-loan summary
};

export const generateSnapshots = (startDate: string, endDate: string, subgraphData: any): Snapshot[] => {
  return [];
};
